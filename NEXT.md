- 여기까지 됨: 터미널 명령에 더해 태스크(빌드 태스크·npm·확장 제공 태스크)도 잡음. 0.1.0 패키징까지 됨
- 다음 할 것: 설치해서 태스크 빌드로 확인. 윈도우 토스트가 실제로 화면에 뜨는지 아직 확인 못 함 — 안 뜨면 알림 설정을 보거나 다른 방법을 찾아야 함
- 막힌 것: CMake Tools 기본 빌드는 내부 프로세스라 어떤 확장도 못 봄. `cmake.buildTask: true` 로 태스크 빌드를 켜야 함
