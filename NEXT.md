- 여기까지 됨: 터미널 명령·태스크 둘 다 알림 확인. 실제 CMake 빌드(4분 9초)에서 윈도우 토스트와 VS Code 알림이 떴음. CMake 는 태스크 경로로 옮겨야 보이고 설정법은 README 에 있음
- 다음 할 것: 회사 PC 에서 쓸지 정하고, 쓰려면 GitHub 에 올려 릴리스 만들기. 실패한 빌드 토스트를 구분할지 정하기
- 막힌 것: 없음. 태스크 방식의 대가로 빌드할 때 패널이 터미널 탭으로 넘어감 — CMake Tools 가 태스크를 다시 만들면서 presentation 을 버리므로 막을 수 없음. 거슬리면 대안은 프로세스 감시(cmake.exe·ninja.exe 가 사라지는 것을 보고 알림)이고, 그러면 내부 프로세스 빌드로 돌아가도 됨
